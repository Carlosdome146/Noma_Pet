const PRODUCTS=[
{id:'roller',name:'Rodillo reutilizable quitapelos',price:22.90,cat:'limpieza',emoji:'🧹',tag:'TOP',desc:'Recoge pelo de sofás, ropa y asientos sin recambios adhesivos.'},
{id:'bottle',name:'Botella 3 en 1 de paseo',price:21.90,cat:'paseo',emoji:'💧',tag:'PASEO',desc:'Agua, bebedero y espacio auxiliar en un formato compacto.'},
{id:'slow',name:'Comedero lento',price:18.90,cat:'hogar',emoji:'🥣',tag:'DIARIO',desc:'Diseño laberinto para alargar el tiempo de comida de forma sencilla.'},
{id:'hammock',name:'Protector de asiento tipo hamaca',price:49.90,cat:'viaje',emoji:'🚗',tag:'VIAJE',desc:'Protección impermeable para el asiento trasero, fácil de colocar y limpiar.'},
{id:'glove',name:'Guante de cepillado',price:14.90,cat:'limpieza',emoji:'🧤',tag:'CUIDADO',desc:'Cepillado cómodo para retirar pelo suelto durante el cuidado diario.'},
{id:'bags',name:'Kit paseo bolsas + dispensador',price:12.90,cat:'paseo',emoji:'♻️',tag:'RECURRENTE',desc:'Un básico ligero y recurrente para los paseos de cada día.'}
];
function money(n){return new Intl.NumberFormat('es-ES',{style:'currency',currency:'EUR'}).format(n)}
function cart(){return JSON.parse(localStorage.getItem('noma-cart')||'[]')}
function saveCart(c){localStorage.setItem('noma-cart',JSON.stringify(c));syncBadge()}
function syncBadge(){document.querySelectorAll('.cartbadge').forEach(el=>el.textContent=cart().reduce((a,x)=>a+x.qty,0))}
function add(id,qty=1){const c=cart();const hit=c.find(x=>x.id===id);hit?hit.qty+=qty:c.push({id,qty});saveCart(c);const b=document.querySelector('[data-add="'+id+'"]');if(b){const old=b.textContent;b.textContent='✓ Añadido';setTimeout(()=>b.textContent=old,1000)}}
function productCard(p){return `<article class="product" data-cat="${p.cat}"><a href="producto.html?id=${p.id}" class="photo"><span class="tag">${p.tag}</span>${p.emoji}</a><div class="product-body"><h3>${p.name}</h3><p>${p.desc}</p><div class="price"><div><strong>${money(p.price)}</strong><div class="meta">IVA y envío: por configurar</div></div><button class="btn primary" data-add="${p.id}" onclick="add('${p.id}')">Añadir</button></div></div></article>`}
function renderProducts(target='products',limit){const el=document.getElementById(target);if(!el)return;el.innerHTML=PRODUCTS.slice(0,limit||PRODUCTS.length).map(productCard).join('')}
function initFilters(){document.querySelectorAll('.filter').forEach(f=>f.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));f.classList.add('active');const cat=f.dataset.filter;document.querySelectorAll('.product').forEach(p=>p.style.display=(cat==='all'||p.dataset.cat===cat)?'flex':'none')}))}
function renderDetail(){const el=document.getElementById('detail');if(!el)return;const id=new URLSearchParams(location.search).get('id')||'roller';const p=PRODUCTS.find(x=>x.id===id)||PRODUCTS[0];document.title=p.name+' — NÓMA PET';el.innerHTML=`<div class="bigphoto">${p.emoji}</div><div class="detail"><span class="eyebrow">Selección funcional</span><h1>${p.name}</h1><div class="pricebig">${money(p.price)}</div><p class="lead">${p.desc}</p><ul class="bullets"><li>✓ Producto ligero y sencillo de explicar</li><li>✓ Sin electrónica ni consumibles regulados</li><li>✓ Ideal para demostraciones en vídeo corto</li><li>✓ Proveedor y plazo UE pendientes de homologación</li></ul><div style="display:flex;gap:12px;align-items:center;margin:24px 0"><div class="qty"><button onclick="changeQty(-1)">−</button><span id="qty">1</span><button onclick="changeQty(1)">+</button></div><button class="btn primary" onclick="add('${p.id}',Number(document.getElementById('qty').textContent))">Añadir al carrito</button></div><div class="notice">Prototipo: antes de vender, verificaremos muestra, materiales, operador responsable en la UE, documentación GPSR, coste real puesto en España y política de devolución del proveedor.</div></div>`}
function changeQty(d){const q=document.getElementById('qty');q.textContent=Math.max(1,Number(q.textContent)+d)}
function renderCart(){const list=document.getElementById('cart-items'),sum=document.getElementById('summary');if(!list||!sum)return;const c=cart();if(!c.length){list.innerHTML='<div class="feature"><h3>Tu carrito está vacío</h3><p>Prueba a añadir uno de los productos del catálogo.</p><a class="btn primary" href="tienda.html">Ir a tienda</a></div>';sum.innerHTML='';return}let subtotal=0;list.innerHTML=c.map(x=>{const p=PRODUCTS.find(y=>y.id===x.id);subtotal+=p.price*x.qty;return `<div class="cartitem"><div class="cartthumb">${p.emoji}</div><div><b>${p.name}</b><div class="meta">${money(p.price)} · Cantidad ${x.qty}</div><button style="border:0;background:none;padding:6px 0;color:#8a4a34;cursor:pointer" onclick="removeItem('${x.id}')">Eliminar</button></div><div class="right"><b>${money(p.price*x.qty)}</b></div></div>`}).join('');sum.innerHTML=`<h3>Resumen</h3><div class="row"><span>Productos</span><b>${money(subtotal)}</b></div><div class="row"><span>Envío</span><span>Por calcular</span></div><div class="row total"><span>Total</span><span>${money(subtotal)}</span></div><button class="btn primary" style="width:100%;margin-top:14px" onclick="alert('La integración de Stripe se hará cuando tengamos proveedor, datos fiscales y dominio definitivos.')">Continuar al pago</button><p class="tiny">Checkout de demostración. No procesa pagos.</p>`}
function removeItem(id){saveCart(cart().filter(x=>x.id!==id));renderCart()}
function adminRows(){const tb=document.getElementById('adminRows');if(!tb)return;const stored=JSON.parse(localStorage.getItem('noma-admin-products')||'null')||PRODUCTS;tb.innerHTML=stored.map(p=>`<tr><td>${p.emoji} ${p.name}</td><td>${p.cat}</td><td>${money(p.price)}</td><td><span class="status">Publicado</span></td><td><button class="btn secondary" onclick="alert('En la versión Cloudflare D1 este botón abrirá el formulario real de edición.')">Editar</button></td></tr>`).join('')}
document.addEventListener('DOMContentLoaded',()=>{syncBadge();renderProducts();renderProducts('featured',3);initFilters();renderDetail();renderCart();adminRows()});
